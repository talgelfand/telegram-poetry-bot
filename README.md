# A Telegram bot to get a random poem 📖

## The bot has two commands:

- `/random` - will fetch a random poem and send it as a message
- `/author <author name>` - will fetch a random poem from the chosen author
